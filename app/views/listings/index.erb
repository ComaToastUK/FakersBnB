<ul id='listings'>
    <% @listings.each do |listing| %>
    <%= listing.title.capitalize %>
    <%= listing.location %>
    <%= listing.price %>
    <img src="<%=listing.imageurl%>" width="300px" />
    <%end%>
  </ul>
